# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.HomeController.index

# ~~~
# Todoの作成を行うためのAPIを作成
# URLを作成する時        -> このページを見ただけで何を行うためのAPIなのかが一眼でわかるように作成する
# Controllerのメソッド名 -> 上記と同じでなんの処理を行うのかを一眼でわかるようにする

# GET リクエストメソッドでTodoを作成するためのフォームを表示させる
# URL 命名はtodoの作成用フォームを表示させるという感じ(英語はわからないので許してください)
#
# Controllerのメソッド名はshowCreatFormで作成用フォームを表示させるアクションを行う
GET     /todo/show/createform       controllers.todo.TodoCreateController.showCreateForm

# POST 上記のフォームで入力した内容をデータベースへ送るリクエストメソッド
# URL  命名はこのAPIにきた段階で登録処理は完了しているので、todoの作成処理が完了しているという感じ
#
# Controllerのメソッド名はcreatで新規Todoを作成するアクションを行う
POST    /todo/complete/create       controllers.todo.TodoCreateController.create

# 処理の流れは登録フォームを作成するURLにアクセス(上記GETリクエストメソッドのURL)
# 例: localhost:4200/todo/show/createform
#　↓
# フォームを入力し上記controllerのcreatメソッドにデータを送る
#  ↓
# createメソッドで登録処理が終わり、Resultで上記URLのページが表示される
# 例: localhost:4200/todo/complete/create
#
# POSTの順番がわかりづらいかもしれませんが、色々触って慣れましょうw
# ~~~

GET     /todo/show/get-all         controllers.todo.TodoReadController.showGetAll
GET     /todo/show/updateform      controllers.todo.TodoUpdateController.showUpdateForm(id: Long)
POST    /todo/update/complite      controllers.todo.TodoUpdateController.upUpdate(id: Long)
# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
